name: Concurrency 2
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        type: environment
        required: true

concurrency: concurrency-1 # string called concurrency group (only one with that name at a time)
jobs:
  sleep:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 10